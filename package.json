{
  "name": "budget",
  "version": "1.0.0",
  "description": "### To run the application",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently --kill-others 'yarn run devFrontEnd' 'yarn run devBackEnd'",
    "devDocker": "docker-compose up --build --force-recreate --no-deps",
    "devFrontEnd": "cd client && yarn run start",
    "devBackEnd": "cd server && yarn run dev",
    "devDockerBackEnd": "cd server && yarn run devDocker",
    "removeAllImages": "docker images -a -q | xargs docker rmi -f",
    "setup": "yarn install && cd client && yarn install && cd ../server && yarn install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/csteven15/budget.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/csteven15/budget/issues"
  },
  "homepage": "https://github.com/csteven15/budget#readme",
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}