# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Amount {
  _id: ID!
  amount: Float!
  date: DateTime!
  entryId: ID!
  paid: Boolean!
}

input CreateAmountInput {
  amount: Float!
  date: DateTime
  entryId: ID!
  paid: Boolean
}

input CreateEntryInput {
  budgetedAmount: Float!
  endDate: DateTime
  frequency: Int
  name: String!
  startDate: DateTime
  type: Int!
  userId: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Entry {
  _id: ID!
  amounts: [Amount!]
  budgetedAmount: Float!
  createdAt: DateTime!
  endDate: DateTime
  name: String!
  startDate: DateTime
  type: Int!
  userId: String!
}

input GetAmountInput {
  _id: ID
  amount: Float
  date: DateTime
  entryId: ID
  paid: Boolean
}

input GetEntryDateFilterInput {
  endDate: DateTime
  startDate: DateTime
}

input GetEntryInput {
  _id: ID
  name: String
  type: Int
  userId: String
}

type Mutation {
  createAmount(payload: CreateAmountInput!): Amount!
  createEntry(payload: CreateEntryInput!): Entry!
  deleteAmount(_id: ID!): Amount!
  deleteEntry(_id: ID!): Entry!
  updateAmount(payload: UpdateAmountInput!): Amount!
  updateEntry(payload: UpdateEntryInput!): Entry!
}

type Query {
  amount(_id: ID!): Amount!
  amounts(filters: GetAmountInput): [Amount!]!
  entries(filter: GetEntryDateFilterInput, payload: GetEntryInput): [Entry!]!
  entry(_id: ID!): Entry!
}

input UpdateAmountInput {
  _id: ID!
  amount: Float
  date: DateTime
  entryId: ID
  paid: Boolean
}

input UpdateEntryInput {
  _id: ID!
  endDate: DateTime
  name: String
  startDate: DateTime
  type: Int
  userId: String
}
